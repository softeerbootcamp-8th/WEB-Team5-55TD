## 설치 경로: /etc/datadog-agent/environment (datadog-agent.service 의 EnvironmentFile)
## MySQL DBM(mysql-dbm.yaml)과 동일하게 애플리케이션 배포와 분리해 수동 설치한다.
## journald-logs.yaml 과 함께 설치해야 실제로 로그 수집이 켜진다.
DD_LOGS_ENABLED=true
