package com.ootd.pickup.global.event.redis;

/**
 * Redis Pub/Sub 채널을 구독해 알림 이벤트를 핸들러로 넘기는 구독자.
 *
 * <p>아직 골격만 있다. 채널로 온 알림은 아무 곳에도 닿지 않는다.
 */
public class RedisEventSubscriber {
  // TODO: 채널을 구독해 EventType.notificationEventClass() 로 역직렬화한 뒤 핸들러로 넘긴다
}
